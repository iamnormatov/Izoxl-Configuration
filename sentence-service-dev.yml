spring:
  application:
    name: sentence-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: azizjon
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
server:
#  servlet:
#    context-path: /sentence-service
  port: 8010